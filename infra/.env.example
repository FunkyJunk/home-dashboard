PORT=3000

# Home Assistant - generate a long-lived access token from your HA profile page
HOME_ASSISTANT_URL=http://homeassistant:8123
HOME_ASSISTANT_TOKEN=

# Weather (Open-Meteo, no key needed - just coordinates)
LAT=
LON=

# Google Calendar / Gmail OAuth (from get-refresh-token.js)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Reminders (Scratch Pad card) - backed by a Home Assistant todo entity, with
# recurrence and iPhone push handled by backend/src/reminders.js.
# Both are auto-discovered when blank: the first todo.* entity, and the first
# notify.mobile_app_* service. Set them explicitly once a second to-do list or
# a second phone exists, since "first" is then a coin toss.
HA_TODO_ENTITY=
HA_NOTIFY_SERVICE=
# All-day reminders have no time of their own, so they push at this hour (0-23)
# instead of at midnight.
REMINDER_ALLDAY_HOUR=8
# How often the backend checks for due reminders, in seconds.
REMINDER_POLL_SECONDS=60

# Label printer (IPP). Set this and the dashboard sends 4x6 labels straight
# from the NAS instead of opening the browser's print dialog. Find the URI with
# `curl -X POST http://<nas>:3000/api/printer/probe -d '{"host":"<printer-ip>"}'
# -H 'Content-Type: application/json'`, which tries the conventional IPP paths.
# Leave blank to keep using the browser dialog.
LABEL_PRINTER_URI=
# Optional IPP media keyword, e.g. na_index-4x6_4x6in. GET /api/printer lists
# what the printer reports as media-supported. Blank leaves the printer on its
# own default.
LABEL_PRINTER_MEDIA=

# Plex Media Server (Roku Devices card) - only needed for the rich
# poster/title/watched-left-end metadata shown when a Roku is playing
# Plex specifically; every other app just shows its name (Roku's own API
# doesn't expose more than that for any other app). Get the token by
# signing into app.plex.tv, opening any item, ... -> Get Info -> View XML,
# and copying X-Plex-Token from the resulting URL.
PLEX_URL=http://192.168.1.97:32400
PLEX_TOKEN=

# Receipt line-item/total extraction (console.anthropic.com -> API keys).
# Optional - without it, receipt scanning falls back to regex-based
# extraction, which misses items in many vendor email formats.
ANTHROPIC_API_KEY=
