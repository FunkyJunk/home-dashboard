node_modules/
.env
infra/.env
infra/volumes/
infra/ssl/
*.log
.DS_Store
credentials.json
nest-credentials.json

# Per-machine Claude Code permissions. Only ever ignored here by a global
# gitignore before, so it would have been committed on any machine without one.
.claude/settings.local.json